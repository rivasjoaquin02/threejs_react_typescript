// backgound decoration
import Elipses from "../bg/ElipsesBg";

// types
import { CompaniesType } from "../types";

const Home = () => {
    return (
        <>
            {/* <Elipses colors={colors} /> */}
            <div className=" 600 my-auto flex h-screen sm:flex-col md:flex-row">
                {/* <Info name={name} description={description} website={website} />
                <MiddleSection asset={scene.asset} bg={scene.bg} aka={aka} />
                <Metrics metrics={metrics} colors={colors} /> */}
            </div>
        </>
    );
};

export default Home;
